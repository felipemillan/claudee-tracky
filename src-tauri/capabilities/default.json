{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and popup windows",
  "windows": ["main", "popup"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
